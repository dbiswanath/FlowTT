// src/components/SectionTitle.js
import React from 'react';

const SectionTitle = ({ title }) => {
  return (
    <h4 className="text-warning fw-bold ms-4 mt-4">{title}</h4>
  );
};

export default SectionTitle;
